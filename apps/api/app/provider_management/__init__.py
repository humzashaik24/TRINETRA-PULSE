"""Phase 23 — AI/media provider management.

Exposes the capability matrix, the read-only registry that resolves the active
provider for a capability, connection testing, and the admin CRUD service.
"""
