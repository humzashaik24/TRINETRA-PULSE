# Render Python runtime dependency manifest.
# Mirrors apps/api/pyproject.toml [project.dependencies]. Render runs
# `pip install -r requirements.txt` with apps/api as the working directory
# (see render.yaml). In production APP_ENV must be `production`.

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9
httpx>=0.27.0
tenacity>=8.2.0
structlog>=24.1.0

# Blockchain evidence anchoring (Phase 21): real EVM testnet provider.
# Install within the backend extra; the `mock` provider (default) does not need it.
web3>=6.20.0
